import React from 'react';

export default function Header() {
    return (
        <section id="header" className="section is-small has-text-centered">
            <h1 className="title">A* Demo App</h1>
            <p className="subtitle">(work in progress)</p>
        </section>
    );
}
